spring:
  application:
    name: reactive-mongodb-service
  data:
    mongodb:
       port: 27018 # using different port for embedded mongodb server
       host: localhost
       database: test

server:
  port: 9100

logging:
  level:
     org:
      springframework:
        data:
          mongodb:
             core: DEBUG
